@model IEnumerable<Mills_4800.Models.FlightDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numberOfTickets)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numberOfTickets)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.flightDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.flightDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.flightDetailID })
        </td>
    </tr>
}

</table>
